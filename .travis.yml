language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"

install:
    - pip install -U -r test_requirements.txt
    - pip install -U -r requirements.txt
    - pip install -e .

script:
    - py.test

# after_success:
#     - coveralls
